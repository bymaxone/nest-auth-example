{
  "$comment": "TEMPORARY — delete during Phase 3 (see docs/DEVELOPMENT_PLAN.md §Phase 3). Probe package validating each @bymax-one/nest-auth subpath export resolves with the project's tsconfig + moduleResolution.",
  "name": "@nest-auth-example/_probe",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@bymax-one/nest-auth": "^1.0.4"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
