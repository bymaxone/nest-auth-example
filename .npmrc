frozen-lockfile=true
# Hoist tailwindcss so eslint-plugin-tailwindcss worker threads can resolve it
# via dynamic import() regardless of pnpm's strict isolation model.
public-hoist-pattern[]=tailwindcss
